<?xml version="1.0" encoding="iso-8859-1"?>
<!DOCTYPE muclient>

<!-- Bits of this plugin and ideas were borrowed and remixed from the MUSHclient community. http://www.gammon.com.au/forum/?id=9385 and others. -->
<!-- Modifications for Aardwolf and extra awesome sauce added by Fiendish with help from Orogan -->

<muclient>
    <plugin
    name="Chat_Capture_Miniwindow"
    author="Fiendish"
    id="b555825a4a5700c35fa80780"
    language="Lua"
    purpose="Move chats to a miniwindow"
    date_written="2010-04-04"
    requires="4.51"
    version="1.0"
    save_state="y"
    >

</plugin>

<triggers>
    <trigger
    enabled="y"
    match="^{say}(?<msg>.*)"
    regexp="y"
    script="chats"
    omit_from_output="y"
    sequence="100"
    ></trigger>

    <trigger
    enabled="y"
    match="^{chan ch=(?<channel>\w+)}(?<msg>.*)"
    regexp="y"
    script="chats"
    omit_from_output="y"
    sequence="100"
    ></trigger>

    <trigger
    enabled="y"
    match="^{tell}(?<msg>.*)"
    script="chats"
    omit_from_output="y"
    regexp="y"
    sequence="100"
    ></trigger>
</triggers>

<aliases>
    <alias
    script="chat_echo"
    match="^chats echo( on| off)?$"
    enabled="y"
    regexp="y"
    sequence="100"
    ignore_case="y"
    ></alias>
</aliases>

<script>
<![CDATA[

require "movewindow"  -- load the movewindow.lua module
require "copytable"

FONT_NAME = "Dina"
FONT_SIZE = 8
SCROLL_BAR_WIDTH = 15
LEFT_MARGIN = 5
RIGHT_MARGIN = 5
MAX_LINES = 10000 -- how many lines to store in scrollback
TITLE_HEIGHT = 17

-- date_format = "[%d %b %H:%M:%S] "        -- [30 Aug 13:29:49]   date and time 24 hour
-- date_format = "[%d %b %I:%M:%S%p] "  -- [30 Aug 01:20:12PM]     date and time 12 hour
-- date_format = "[%H:%M:%S] "          -- [13:29:08]          time 24 hour
-- date_format = "[%X] "                  -- [1:22:06 PM]            time 12 hour

timestamp = GetVariable("timestamp")
echo = GetVariable("echo")
date_format = GetVariable("date_format")
WINDOW_WIDTH = tonumber(GetVariable("WINDOW_WIDTH"))
WINDOW_HEIGHT = tonumber(GetVariable("WINDOW_HEIGHT"))

-- colours
WINDOW_BACKGROUND_COLOUR = ColourNameToRGB ("black")
WINDOW_TEXT_COLOUR = ColourNameToRGB ("white")
SCROLL_BACKGROUND_COLOUR = ColourNameToRGB ("#E8E8E8")
SCROLL_BAR_COLOUR = ColourNameToRGB ("#111111")
SCROLL_DETAIL_COLOUR = ColourNameToRGB ("black")
WINDOW_BORDER_COLOUR = ColourNameToRGB("#E8E8E8")

-- offset of text from edge
TEXT_INSET = 5

-- where to store the chat line
lines = {}  -- table of recent chat lines
rawlines = {}

lineStart = ""
lineEnd = ""
WINDOW_COLUMNS = ""
WINDOW_LINES = ""

-- pull in telnet option handling
dofile (GetPluginInfo (GetPluginID (), 20) .. "telnet_options.lua")
dofile (GetPluginInfo (GetPluginID (), 20) .. "aardwolf_colors.lua")
CWin = GetPluginID()
CWinResizer = CWin.."resize"
font_height = ""
line_height = ""
windowinfo = ""
startx = ""
starty = ""

function InstallResizeTag()
    movewindow.add_drag_handler (CWinResizer, 0, 0, 0, 0)
    WindowAddHotspot(CWinResizer, "resize", 0, 0, 0, 0, "MouseOver", "CancelMouseOver", "MouseDown", "CancelMouseDown", "MouseUp", "", 6, 0)
    WindowDragHandler(CWinResizer, "resize", "ResizeMoveCallback", "ResizeReleaseCallback", 0)
end

function ResizeMoveCallback()
    posx, posy = WindowInfo (CWinResizer, 17), WindowInfo (CWinResizer, 18)
    if (WindowTextWidth(CWin, "font"..CWin, "WWWCOMMUNICATION")+2*SCROLL_BAR_WIDTH <= WINDOW_WIDTH+posx-startx) then
        WINDOW_WIDTH = WINDOW_WIDTH+posx-startx
        startx = posx
    end
    if (3*SCROLL_BAR_WIDTH+10+line_height+TITLE_HEIGHT <= WINDOW_HEIGHT+posy-starty) then
        WINDOW_HEIGHT = WINDOW_HEIGHT+posy-starty
        starty = posy
    end
    init(false)
end

function ResizeReleaseCallback()
    WINDOW_HEIGHT = TITLE_HEIGHT+(line_height*(WINDOW_LINES-1))+3
    init(true)
end

function OnPluginInstall()
    -- Dummy window to get font characteristics
    check (WindowCreate (CWin, 0, 0, 1, 1, 0, 0, WINDOW_BACKGROUND_COLOUR) )
    check (WindowFont(CWin, "font"..CWin, FONT_NAME, FONT_SIZE))
    font_height = WindowFontInfo (CWin, "font"..CWin, 1) -  WindowFontInfo (CWin, "font"..CWin, 4) + 1
    line_height = font_height+1
    font_width = WindowTextWidth (CWin, "font"..CWin, "W")
    
    -- install the window movement handler, get back the window position
    resizerinfo = movewindow.install (CWinResizer, 6, 6, true)
    windowinfo = movewindow.install (CWin, 6, 2, true, {CWinResizer})

    -- check for Echo/Timestamp/date_format/window size (in pixels) variables, if not there, set them
    if echo == nil then
        echo = "1"
    end
    if timestamp == nil then
        timestamp = "1"
    end
    if date_format == nil then
        date_format = "[%d %b %H:%M:%S] "
    end
    if WINDOW_WIDTH == nil then
        WINDOW_WIDTH = (font_width*80)+SCROLL_BAR_WIDTH -- 80 columns
    end
    if WINDOW_HEIGHT == nil then
        WINDOW_HEIGHT = TITLE_HEIGHT+(line_height*6)+2 -- 6 lines
    end
    init(true)
    OnPluginEnable ()  -- do initialization stuff
end

function init(firstTime)
    -- how many lines and columns will fit?
    WINDOW_LINES = math.ceil((WINDOW_HEIGHT-TITLE_HEIGHT)/line_height)
    WINDOW_COLUMNS = math.ceil((WINDOW_WIDTH-SCROLL_BAR_WIDTH)/font_width)

    WindowCreate(CWin, windowinfo.window_left, windowinfo.window_top, WINDOW_WIDTH, WINDOW_HEIGHT, windowinfo.window_mode, windowinfo.window_flags, WINDOW_BACKGROUND_COLOUR)
    WindowShow(CWin, true)
    
    -- catch for right-click menu and line selection
    WindowAddHotspot(CWin, "textarea", 0, TITLE_HEIGHT, WINDOW_WIDTH-SCROLL_BAR_WIDTH,0, "", "", "MouseDown", "", "MouseUp", "", 2, 0)

    -- add the drag handler so they can move the window around
    movewindow.add_drag_handler (CWin, 0, 0, 0, TITLE_HEIGHT)

    -- scroll bar up/down buttons
    WindowAddHotspot(CWin, "up", WINDOW_WIDTH-SCROLL_BAR_WIDTH, TITLE_HEIGHT, 0, TITLE_HEIGHT+SCROLL_BAR_WIDTH, "MouseOver", "CancelMouseOver", "MouseDown", "CancelMouseDown", "MouseUp", "", 1, 0)
    WindowAddHotspot(CWin, "down", WINDOW_WIDTH-SCROLL_BAR_WIDTH, WINDOW_HEIGHT-(2*SCROLL_BAR_WIDTH), 0, WINDOW_HEIGHT-SCROLL_BAR_WIDTH, "MouseOver", "CancelMouseOver", "MouseDown", "CancelMouseDown", "MouseUp", "", 1, 0)

    -- put the resizer in place
    if (firstTime == true) then
        WindowCreate(CWinResizer, windowinfo.window_left+WINDOW_WIDTH-SCROLL_BAR_WIDTH, windowinfo.window_top+WINDOW_HEIGHT-SCROLL_BAR_WIDTH, SCROLL_BAR_WIDTH, SCROLL_BAR_WIDTH, 0, 6, ColourNameToRGB("pink"))
        WindowShow(CWinResizer, true)
        InstallResizeTag(CWinResizer, SCROLL_BAR_WIDTH)
    else
        WindowPosition(CWinResizer, windowinfo.window_left+WINDOW_WIDTH-SCROLL_BAR_WIDTH, windowinfo.window_top+WINDOW_HEIGHT-SCROLL_BAR_WIDTH, 0, 6)
    end

    lines = {}
    for _,styles in ipairs(rawlines) do
        fillBuffer(styles)
    end
    lineStart = math.max(1, #lines-WINDOW_LINES+2)
    lineEnd = math.max(1, #lines)
    refresh()
end

function OnPluginConnect ()
    -- first time after connect, get tags right
    TelnetOptionOn (TELOPT_CHANNELS)
    TelnetOptionOn (TELOPT_TELLS)
    TelnetOptionOn (TELOPT_SAYS)
end --  OnPluginConnect

function OnPluginClose ()
    -- if enabled
    if GetPluginInfo (GetPluginID (), 17) then
        OnPluginDisable()
    end -- if enabled
end -- OnPluginClose

function OnPluginEnable ()
    WindowShow (CWin, true)
    WindowShow (CWinResizer, true)
    -- if we are connected when the plugin loads, it must have been reloaded whilst playing
    if IsConnected () then
        OnPluginConnect ()
    end -- if already connected
end -- OnPluginEnable

function OnPluginSaveState ()
    -- save window current location for next time  
    SetVariable ("enabled", tostring (GetPluginInfo (GetPluginID (), 17)))
    movewindow.save_state (CWin)
    movewindow.save_state (CWinResizer)
    -- save echo/timestamp status
    SetVariable ("echo", echo)
    SetVariable ("timestamp", timestamp)
    SetVariable("date_format", date_format)
    SetVariable("WINDOW_WIDTH", WINDOW_WIDTH)
    SetVariable("WINDOW_HEIGHT", WINDOW_HEIGHT)
end --  OnPluginSaveState

function OnPluginDisable ()
    TelnetOptionOff (TELOPT_CHANNELS)
    TelnetOptionOff (TELOPT_TELLS)
    TelnetOptionOff (TELOPT_SAYS)
    WindowShow( CWin, false )
    WindowShow( CWinResizer, false)
end --  OnPluginDisable

-- display one line
function Display_Line (line, styles, backfill)
    local left = TEXT_INSET
    if (backfill) then
        WindowRectOp(CWin, 2, 1, TITLE_HEIGHT+(line*line_height)+1, WINDOW_WIDTH-SCROLL_BAR_WIDTH, TITLE_HEIGHT+(line*line_height)+line_height+1, ColourNameToRGB("#333333"))
    end -- backfill
    if (styles) then
        for _, v in ipairs (styles) do
            left = left + WindowText (CWin, "font"..CWin, v.text, left, TITLE_HEIGHT+(line*line_height), 0, 0, v.textcolour)
        end -- for each style run
    end
    Redraw()
end -- Display_Line

-- display all visible lines
function writeLines()
    for count = lineStart, lineEnd do
        Display_Line( count-lineStart, lines[count][1], false )
    end
end

-- clear and redraw
function refresh()
    WindowRectOp(CWin, 2, 0, 0, WINDOW_WIDTH, WINDOW_HEIGHT, WINDOW_BACKGROUND_COLOUR)
    drawStuff()
end

barPos = ""
barSize = ""
totalSteps = ""
function drawStuff()
    -- draw drag bar rectangle
    WindowRectOp (CWin, 1, 0, 0, 0, 0, WINDOW_BORDER_COLOUR)
    
    -- Title bar
    WindowRectOp(CWin, 5, 0, 0, 0, TITLE_HEIGHT, 5,  15 + 0x800)

    -- Title
    WindowText(CWin, "font"..CWin, "COMMUNICATION", ((WINDOW_WIDTH)-(7.5*line_height))/2, (TITLE_HEIGHT-line_height)/2, WINDOW_WIDTH, TITLE_HEIGHT, ColourNameToRGB("black"), false)
    
    -- Scrollbar base
    WindowRectOp(CWin, 2, WINDOW_WIDTH-SCROLL_BAR_WIDTH, TITLE_HEIGHT, WINDOW_WIDTH, WINDOW_HEIGHT, SCROLL_BACKGROUND_COLOUR) -- scroll bar background
    WindowRectOp(CWin, 1, WINDOW_WIDTH-SCROLL_BAR_WIDTH+1, SCROLL_BAR_WIDTH+TITLE_HEIGHT+1, WINDOW_WIDTH-1, WINDOW_HEIGHT-(2*SCROLL_BAR_WIDTH)-1, SCROLL_DETAIL_COLOUR) -- scroll bar background inset rectangle
    WindowRectOp(CWin, 5, (WINDOW_WIDTH-SCROLL_BAR_WIDTH), TITLE_HEIGHT, WINDOW_WIDTH, TITLE_HEIGHT+SCROLL_BAR_WIDTH, 5, 15 + 0x800) -- top scroll button
    WindowRectOp(CWin, 5, (WINDOW_WIDTH-SCROLL_BAR_WIDTH), WINDOW_HEIGHT-(SCROLL_BAR_WIDTH*2), WINDOW_WIDTH, WINDOW_HEIGHT-SCROLL_BAR_WIDTH, 5,  15 + 0x800) -- bottom scroll button

    -- draw triangle in up button
    points = string.format ("%i,%i,%i,%i,%i,%i", (WINDOW_WIDTH-SCROLL_BAR_WIDTH)+3, TITLE_HEIGHT+9,(WINDOW_WIDTH-SCROLL_BAR_WIDTH)+7, TITLE_HEIGHT+5,(WINDOW_WIDTH-SCROLL_BAR_WIDTH)+11, TITLE_HEIGHT+9)
    WindowPolygon (CWin, points,
        ColourNameToRGB("black"), 0, 1,   -- pen (solid, width 1)
        ColourNameToRGB("black"), 0, --brush (solid)
        true, --close
        false)  --alt fill

    -- draw triangle in down button    
    points = string.format ("%i,%i,%i,%i,%i,%i", (WINDOW_WIDTH-SCROLL_BAR_WIDTH)+3, (WINDOW_HEIGHT-SCROLL_BAR_WIDTH)-11,(WINDOW_WIDTH-SCROLL_BAR_WIDTH)+7, (WINDOW_HEIGHT-SCROLL_BAR_WIDTH)-7, (WINDOW_WIDTH-SCROLL_BAR_WIDTH)+11,(WINDOW_HEIGHT-SCROLL_BAR_WIDTH)-11)
    WindowPolygon (CWin, points,
        ColourNameToRGB("black"), 0, 1,   -- pen (solid, width 1)
        ColourNameToRGB("black"), 0, --brush (solid)
        true, --close
        false) --alt fill
    
    -- The scrollbar position indicator
    totalSteps = #lines
    if (totalSteps <= WINDOW_LINES-1) then totalSteps = 1 end
    SCROLL_BAR_HEIGHT = (WINDOW_HEIGHT-(3*SCROLL_BAR_WIDTH)-TITLE_HEIGHT)
    if (not dragscrolling) then
        stepNum = lineStart-1
        barPos = SCROLL_BAR_WIDTH +TITLE_HEIGHT+ ((SCROLL_BAR_HEIGHT/totalSteps) * stepNum)
        barSize = (SCROLL_BAR_HEIGHT/math.max(WINDOW_LINES-1,totalSteps)) * (WINDOW_LINES-1)
        if barSize < 10 then
            barSize = 10
        end
        if barPos+barSize > SCROLL_BAR_WIDTH+TITLE_HEIGHT+SCROLL_BAR_HEIGHT then
            barPos = SCROLL_BAR_WIDTH+TITLE_HEIGHT+SCROLL_BAR_HEIGHT - barSize
        end
        WindowAddHotspot(CWin, "scroller", (WINDOW_WIDTH-SCROLL_BAR_WIDTH), barPos, WINDOW_WIDTH, barPos+barSize, "MouseOver", "CancelMouseOver", "MouseDown", "CancelMouseDown", "MouseUp", "", 1, 0)
        WindowDragHandler(CWin, "scroller", "ScrollerMoveCallback", "ScrollerReleaseCallback", 0)
    end
    WindowRectOp(CWin, 5, (WINDOW_WIDTH-SCROLL_BAR_WIDTH), barPos, WINDOW_WIDTH, barPos+barSize, 5, 15 + 0x800) -- scrollbar position indicator
    
    -- resize tag    
    WindowLine(CWin, WINDOW_WIDTH-SCROLL_BAR_WIDTH+1, WINDOW_HEIGHT-2, WINDOW_WIDTH-2, WINDOW_HEIGHT-SCROLL_BAR_WIDTH+1, ColourNameToRGB ("white"), 0, 2)
    WindowLine(CWin, WINDOW_WIDTH-SCROLL_BAR_WIDTH+2, WINDOW_HEIGHT-2, WINDOW_WIDTH-2, WINDOW_HEIGHT-SCROLL_BAR_WIDTH+2, ColourNameToRGB ("dimgray"), 0, 1)
    WindowLine(CWin, WINDOW_WIDTH-SCROLL_BAR_WIDTH+4, WINDOW_HEIGHT-2, WINDOW_WIDTH-2, WINDOW_HEIGHT-SCROLL_BAR_WIDTH+4, ColourNameToRGB ("white"), 0, 2)
    WindowLine(CWin, WINDOW_WIDTH-SCROLL_BAR_WIDTH+5, WINDOW_HEIGHT-2, WINDOW_WIDTH-2, WINDOW_HEIGHT-SCROLL_BAR_WIDTH+5, ColourNameToRGB ("dimgray"), 0, 1)
    WindowLine(CWin, WINDOW_WIDTH-SCROLL_BAR_WIDTH+7, WINDOW_HEIGHT-2, WINDOW_WIDTH-2, WINDOW_HEIGHT-SCROLL_BAR_WIDTH+7, ColourNameToRGB ("white"), 0, 2)
    WindowLine(CWin, WINDOW_WIDTH-SCROLL_BAR_WIDTH+8, WINDOW_HEIGHT-2, WINDOW_WIDTH-2, WINDOW_HEIGHT-SCROLL_BAR_WIDTH+8, ColourNameToRGB ("dimgray"), 0, 1)
    WindowLine(CWin, WINDOW_WIDTH-SCROLL_BAR_WIDTH+10, WINDOW_HEIGHT-2, WINDOW_WIDTH-2, WINDOW_HEIGHT-SCROLL_BAR_WIDTH+10, ColourNameToRGB ("white"), 0, 2)
    WindowLine(CWin, WINDOW_WIDTH-SCROLL_BAR_WIDTH+11, WINDOW_HEIGHT-2, WINDOW_WIDTH-2, WINDOW_HEIGHT-SCROLL_BAR_WIDTH+11, ColourNameToRGB ("dimgray"), 0, 1)
    if #lines >= 1 then
        writeLines()
    end -- if
    Redraw()
end

function ScrollerMoveCallback(flags, hotspot_id)
    mouseposy = WindowInfo(CWin, 18)
    windowtop = WindowInfo(CWin, 2)
    barPos = math.max(mouseposy-windowtop+clickdelta, SCROLL_BAR_WIDTH+TITLE_HEIGHT)
    if barPos > WINDOW_HEIGHT-(SCROLL_BAR_WIDTH*2)-barSize then
        barPos = WINDOW_HEIGHT-(SCROLL_BAR_WIDTH*2)-barSize
        lineStart = math.max(1,#lines-WINDOW_LINES+2)
        lineEnd = #lines
    else
        lineStart = math.floor((barPos-SCROLL_BAR_WIDTH-TITLE_HEIGHT)/(SCROLL_BAR_HEIGHT/totalSteps)+1)
        lineEnd = math.min(lineStart + WINDOW_LINES-2, #lines)
    end
    refresh()
end

function ScrollerReleaseCallback(flags, hotspot_id)
    dragscrolling = false
    refresh()
end

function fillBuffer(rawstyles)
    local avail = 0
    local line_styles
    local beginning = true
    -- keep pulling out styles and trying to fit them on the current line
    local styles = copytable.deep (rawstyles)
    while #styles > 0 do
        if avail <= 0 then -- no room available? start new line
            -- remove first line if filled up
            if #lines >= MAX_LINES then
                table.remove (lines, 1)
            end -- if 
            avail = WINDOW_WIDTH - (TEXT_INSET * 2) - 9
            line_styles = {}
            add_line( line_styles, beginning )
            beginning = false
        end -- line full

        -- get next style, work out how long it is
        local style = table.remove (styles, 1)
        local width = WindowTextWidth (CWin, "font"..CWin, style.text)

        -- if it fits, copy whole style in
        if width <= avail then
            table.insert (line_styles, style)
            avail = avail - width
        else -- otherwise, have to split style   
            -- look for trailing space (work backwards). remember where space is
            local col = style.length - 1
            local split_col
            -- keep going until out of columns
            while col > 1 do
                width = WindowTextWidth (CWin, "font"..CWin, style.text:sub (1, col)) 
                if width <= avail then
                    if not split_col then
                        split_col = col  -- in case no space found, this is where we can split
                    end -- if
                    -- see if space here
                    if style.text:sub (col, col) == " " then
                        split_col = col
                        break
                    end -- if space
                end -- if will now fit
                col = col - 1
            end -- while
          
            -- if we found a place to split, use old style, and make it shorter. Also make a copy and put the rest in that
            if split_col then
                table.insert (line_styles, style)
                local style_copy = copytable.shallow (style)
                style.text = style.text:sub (1, split_col)
                style.length = split_col 
                style_copy.text = style_copy.text:sub (split_col + 1)
                style_copy.length = #style_copy.text
                table.insert (styles, 1, style_copy)
            elseif next (line_styles) == nil then
                table.insert (line_styles, style)
            else
                table.insert (styles, 1, style)
            end -- if    
            avail = 0  -- now we need to wrap     
        end -- if could not fit whole thing in
    end -- while we still have styles over
end

-- Main capture routine
function chats (name, line, wildcards, styles)
    -- strip out the tag
    tag_length = string.find(styles[1].text,"}")
    styles[1].text = string.sub(styles[1].text, tag_length+1)
    styles[1].length = styles[1].length-tag_length
    
    -- echo in this world as well if the user wants
    if echo == "1" then
        for _, v in ipairs (styles) do
            ColourTell (RGBColourToName (v.textcolour),RGBColourToName (v.backcolour),v.text)
        end -- for each style run
        Note ("")  -- wrap up line
    end -- echo wanted

    -- inject timestamp if wanted
    if timestamp == "1" then
        tstamp = os.date (date_format)
        styles[1].text = tstamp..styles[1].text
        styles[1].length = styles[1].length+string.len(tstamp)
    end -- if
    -- store the raw lines for use during resizing
    if #rawlines >= MAX_LINES then
        table.remove(rawlines, 1)
    end
    table.insert(rawlines, styles)
    
    fillBuffer(styles)
    refresh( )
end -- chats

function add_line ( line, is_beginning_of_message )
    -- add new line
    table.insert (lines, {line} )
    lines[#lines][2] = is_beginning_of_message

    -- advance the count
    if #lines >= WINDOW_LINES then
        lineStart = lineStart + 1
    end -- if
        
    if #lines > 1 then
        lineEnd = lineEnd + 1
    end -- if
end -- add_line

keepscrolling = false
require "wait"

function scrollbar(calledBy)
    wait.make (function()
        while keepscrolling == true do
            if calledBy == "up" then
                if (lineStart > 1) then
                    lineStart = lineStart - 1
                    lineEnd = lineEnd - 1
                    WindowRectOp(CWin, 5, (WINDOW_WIDTH-SCROLL_BAR_WIDTH), TITLE_HEIGHT, 0, TITLE_HEIGHT+SCROLL_BAR_WIDTH, 10,  15 + 0x800) -- up arrow pushed
                    points = string.format ("%i,%i,%i,%i,%i,%i", (WINDOW_WIDTH-SCROLL_BAR_WIDTH)+3, TITLE_HEIGHT+9,(WINDOW_WIDTH-SCROLL_BAR_WIDTH)+7, TITLE_HEIGHT+5,(WINDOW_WIDTH-SCROLL_BAR_WIDTH)+11, TITLE_HEIGHT+9)
                    WindowPolygon (CWin, points,
                        ColourNameToRGB("black"), 0, 1, -- pen (solid, width 1)
                        ColourNameToRGB("black"), 0, -- brush (solid)
                        true, -- close
                        false) -- alt fill
                else
                    keepscrolling = false
                end
            elseif calledBy == "down" then
                if (lineEnd < #lines) then
                    lineStart = lineStart + 1
                    lineEnd = lineEnd + 1
                WindowRectOp(CWin, 5, (WINDOW_WIDTH-SCROLL_BAR_WIDTH), WINDOW_HEIGHT-(SCROLL_BAR_WIDTH*2), 0, WINDOW_HEIGHT-SCROLL_BAR_WIDTH-1, 10,  15 + 0x800) -- down arrow pushed
                points = string.format ("%i,%i,%i,%i,%i,%i", (WINDOW_WIDTH-SCROLL_BAR_WIDTH)+3, (WINDOW_HEIGHT-SCROLL_BAR_WIDTH)-11,(WINDOW_WIDTH-SCROLL_BAR_WIDTH)+7, (WINDOW_HEIGHT-SCROLL_BAR_WIDTH)-7, (WINDOW_WIDTH-SCROLL_BAR_WIDTH)+11,(WINDOW_HEIGHT-SCROLL_BAR_WIDTH)-11) -- draw triangle in up button
                WindowPolygon (CWin, points,
                    ColourNameToRGB("black"), 0, 1, -- pen (solid, width 1)
                    ColourNameToRGB("black"), 0, -- brush (solid)
                    true, -- close
                    false) -- alt fill
                else
                    keepscrolling = false
                end
            end
            wait.time(0.1)
            refresh()
        end
    end)
end

function GetAllBufferedMessages()
    local t = {}
    for _,styles in ipairs(rawlines) do
        table.insert(t, ColourConvertOneLine(copytable.deep(styles), 1, 99999)) -- 99999 basically means don't truncate lines
    end
    SetClipboard(table.concat(t,"\r\n"))
end

function GetBufferedMessage(xpos, ypos)
    windowline = math.floor(((ypos-line_height)/line_height)+1)-1
    text = ""
    if (#lines > windowline) then
        local line = windowline+lineStart
        -- go to beginning of message
        while lines[line][2] ~= true and line > 1 do
            line = line - 1
        end -- while
        -- first line
        local styles = copytable.deep(lines[line][1])
        if (line-lineStart+1 > 0) then
            Display_Line (line-lineStart, styles, true)
        end
        text = ColourConvertOneLine(styles, 1, 99999) -- 99999 fixes a potential bug with WINE
        -- remaining lines
        line = line + 1
        while line <= #lines and lines[line][2] ~= true do
            local styles = copytable.deep(lines[line][1])
            if (line-lineStart+1 > 0 and line-lineStart < WINDOW_LINES) then
                Display_Line (line-lineStart, styles, true)
            end
            text = text..ColourConvertOneLine(styles, 1, 99999) -- 99999 fixes a potential bug with WINE
            line = line + 1
        end -- while
        SetClipboard(text)
    end
end

function MouseOver(flags, hotspot_id)
    keepscrolling = false
    refresh()
end

function CancelMouseOver(flags, hotspot_id)
    keepscrolling = false
    refresh()
end

function MouseDown(flags, hotspot_id)
    if (hotspot_id == "resize") then
        startx, starty = WindowInfo (CWinResizer, 17), WindowInfo (CWinResizer, 18)
    elseif (hotspot_id == "scroller") then
        clickdelta = WindowHotspotInfo(CWin, "scroller", 2)-WindowInfo (CWin, 15)
        dragscrolling = true
    elseif (hotspot_id == "textarea" and flags == 0x10) then
        GetBufferedMessage(WindowInfo(CWin, 14), WindowInfo(CWin,15))
    else
        keepscrolling = true
        scrollbar(hotspot_id)
    end
end

function CancelMouseDown(flags, hotspot_id)
    keepscrolling = false
    refresh()
end

function MouseUp(flags, hotspot_id)
    if (hotspot_id == "textarea" and flags == 0x20) then
        -- build menu for current state
        right_click_menu()
    else
        keepscrolling = false
        refresh()
    end
end

function chat_echo (name, line, wildcards)
    if wildcards [1] == false then
        echo = not echo
    elseif wildcards [1]:lower () == " on" then
        echo = "1"
    elseif wildcards [1]:lower () == " off" then
        echo = "0"
    end -- if

    if echo == "1" then
        ColourNote ("yellow", "", "Echoing chats in main window ENABLED.")
    else
        ColourNote ("yellow", "", "Echoing chats in main window DISABLED.")
    end -- if
end -- chat_echo

-- right click menu
function right_click_menu ()
    menustring ="Copy All To Clipboard|Change Font"
    if echo == "1" then
        menustring = (menustring .."|Turn Echo Off")
    else
        menustring = (menustring.."|Turn Echo On")
    end --if
    if timestamp == "1" then
        menustring = (menustring.."|Turn Timestamp Off")
    else
        menustring = (menustring.."|Turn Timestamp On")
    end -- if
    result = WindowMenu (CWin, 
        WindowInfo (CWin, 14),  -- x position
        WindowInfo (CWin, 15),   -- y position
        menustring) -- content

    if result == "Copy All To Clipboard" then
        GetAllBufferedMessages()
    end

    if result == "Change Font" then
        wanted_font = utils.fontpicker (FONT_NAME, FONT_SIZE) --font dialog
        if wanted_font then
            FONT_NAME = wanted_font.name
            FONT_SIZE = wanted_font.size
            SetVariable ("font", FONT_NAME)
            SetVariable ("font_size", FONT_SIZE)
            OnPluginInstall()
        end
    end -- if

    if result == "Turn Echo Off" then
        echo = "0"
        ColourNote ("yellow", "", "Echoing chats in main window DISABLED.")
    end
    if result == "Turn Echo On" then
        echo = "1"
        ColourNote ("yellow", "", "Echoing chats in main window ENABLED.")
    end
    if result == "Turn Timestamp Off" then
        timestamp = "0"
        ColourNote ("yellow", "", "Timestamp in communication window DISABLED.")
    end
    if result == "Turn Timestamp On" then
        timestamp = "1"
        subresult = WindowMenu (CWin, 
            WindowInfo (CWin, 14),  -- x
            WindowInfo (CWin, 15),   -- y
            "^FORMAT|30 Aug 13:29:49|30 Aug 01:20:12PM|13:29:08|1:22:06 PM")
        if subresult == "30 Aug 13:29:49" then
            date_format = "[%d %b %H:%M:%S] "
        elseif subresult == "30 Aug 01:20:12PM" then
           date_format = "[%d %b %I:%M:%S%p] "
        elseif subresult == "13:29:08" then
            date_format = "[%H:%M:%S] " 
        elseif subresult == "1:22:06 PM" then
            date_format = "[%I:%M:%S%p] " 
        end
        ColourNote ("yellow", "", "Timestamp in communication window ENABLED.")
    end
end -- right_click_menu
]]>
</script>
</muclient>
